import { View as ReactNativeView, Button as ReactNativeButton } from 'react-native'
import { styled } from 'nativewind'

export const View = styled(ReactNativeView)
export const Button = styled(ReactNativeButton)
